here we are using a package called json server-a fake rest api just using a json file.
here the top level property is considered as resources.
next use the json file:
two option:-either install json server package locally   or
use npx library : npx json-server --watch data/db.json --port 8000 
//this is gonna create us a api endpoint
.
.refer this to actually create a db.json 
https://github.com/typicode/json-server
i did:npm install -g json-server
then:json-server --watch db.json --port 3004
